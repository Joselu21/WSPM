## Main function of the Price Monitor package
# It needs to detect the parameters and respond to them
# The first parameter is optional and are products < -P  file > or < -P  [file,file,file...] >
# The second parameter is optional and represent user files to evaluate its thresholds < -U file > or < -U [file,file,file] >
# The 2 parameters are optional but it must have at least one of them.
# All the parameters are passed to the PriceMonitor class
